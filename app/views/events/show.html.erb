<section class="team-section text-center my-5">

  <!-- Section heading -->
  <h2 class="h1-responsive font-weight-bold my-5"><%=@event.title%></h2>




<div style="border:1px solid grey;padding:20px;">
  <p>Titre de l'event : <strong><%=@event.title%></strong></p>
  <p>Contenu : <strong><i><%=@event.description%></i></strong></p>
  <p>Admin : <strong><%= link_to "#{@event.admin.first_name}", user_path(@event.admin.id) %></strong></p>
  <p>Start Date : <strong><%=@event.created_at%></strong></p>
</div>
<%if current_user == @event.admin%>
<%= link_to "Éditer l'Event", edit_event_path, class:"btn btn-primary mb-3 mt-3" %>
<%= form_tag url_for(action: 'destroy'), method: "delete" do %>
      <%= submit_tag "Delete Event",class:"btn btn-danger mb-3" %>
<% end %>
<%end%>
  </section>